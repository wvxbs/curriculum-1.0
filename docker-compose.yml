version: '3.9'

services:
  web:
    image: wvxbs/curriculum
    build: .
    environment:
      NODE_ENV: production,
      REACT_ENV:  production,
      API_URL: ${API_URL} 
    ports:
      - 3000:3000
