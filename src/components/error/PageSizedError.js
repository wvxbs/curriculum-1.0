const PageSizedError = (props) => {
    return (
        <div className="page-sized-error">
            <h1 className="page-sized-error-text title is -1">
                {props.title}
            </h1>
        </div>
    )
}

export default PageSizedError